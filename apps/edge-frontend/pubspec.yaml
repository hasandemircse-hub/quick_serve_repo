name: quickserve_edge_frontend
description: QuickServe edge staff frontend
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.2.1
  shared_frontend:
    path: ../../packages/shared-frontend

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/edge_frontend.env
