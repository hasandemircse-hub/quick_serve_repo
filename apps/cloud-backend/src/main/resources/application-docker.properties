spring.datasource.url=jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:quickserve}
spring.datasource.username=${DB_USERNAME:quickserve}
spring.datasource.password=${DB_PASSWORD:quickserve123}

app.jwt.secret=${JWT_SECRET:QuickServe2024SuperSecretKeyForJWTTokenGeneration32BytesMin!}
app.frontend-url=${FRONTEND_URL:http://localhost:3000}
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000}

spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}

app.sms.netgsm.usercode=${NETGSM_USERCODE:}
app.sms.netgsm.password=${NETGSM_PASSWORD:}
app.sms.netgsm.msgheader=${NETGSM_HEADER:QUICKSERVE}

app.superadmin.username=${SUPERADMIN_USERNAME:superadmin}
app.superadmin.password=${SUPERADMIN_PASSWORD}
app.superadmin.email=${SUPERADMIN_EMAIL}
app.superadmin.phone=${SUPERADMIN_PHONE:}

logging.level.com.quickserve=INFO
spring.jpa.show-sql=false

management.health.mail.enabled=false

# Docker / VM lab: edge JWT olmadan bootstrap (root .env → QUICKSERVE_DEV_INSECURE_EDGE_BRIDGE)
app.dev.insecure-edge-cloud-bridge=${QUICKSERVE_DEV_INSECURE_EDGE_BRIDGE:false}
